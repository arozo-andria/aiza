node_modules
.next
.git

# Benchmark: Python env and downloaded model weights (multi-GB) never
# belong in the deployed app bundle. benchmark/audio ships deliberately -
# it's what /record plays back read-only in production (see app/api/record).
benchmark/venv
benchmark/models_cache

*.log
.DS_Store
